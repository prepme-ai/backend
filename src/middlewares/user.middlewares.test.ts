// TODO: Write tests for per func.
// TODO: Configure package json.

import { describe, expect, test } from 'jest';

describe('Register User Validated', () => {
  test('', () => {
    expect();
  });
});
